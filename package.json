{
  "name": "note-manager",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "license": "MIT",
  "author": "David Moriarty",
  "homepage": "https://note-manager.davidmoriarty.dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/davidmoriarty/note-manager.git"
  },
  "description": "Full-stack notes application with JWT auth, rotating refresh tokens, and a Bun + Hono + React monorepo architecture",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,css}": "biome check --write --no-errors-on-unmatched"
  },
  "workspaces": [
    "./server",
    "./client",
    "./shared"
  ],
  "keywords": [
    "bun",
    "hono",
    "react",
    "vite",
    "tanstack-router",
    "prisma",
    "postgresql",
    "jwt-authentication",
    "refresh-token-rotation",
    "monorepo",
    "turbo"
  ],
  "scripts": {
    "prepare": "husky",
    "hooks:pre-commit": "lint-staged",
    "fmt": "biome check . --write",
    "lint": "biome check .",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "build": "turbo build",
    "check": "bun run lint && bun run type-check && bun run build",
    "dev": "turbo dev",
    "dev:client": "turbo dev --filter=client",
    "dev:server": "turbo dev --filter=server",
    "build:client": "turbo build --filter=client",
    "build:server": "turbo build --filter=server",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@tailwindcss/typography": "^0.5.20",
    "@tanstack/react-query-devtools": "^5.101.4",
    "@types/bun": "^1.3.14",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "ts-prune": "^0.10.3",
    "turbo": "^2.10.8"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "hono": "^4.12.33"
  }
}
